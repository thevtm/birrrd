<h1>You can see the index of all our birds!!</h1>

<h2>Geocode should be on the right side</h2>


<% @birds.each do |bird| %>
  <h2> <%= bird.name %> </h2>
  <h3> <%= bird.location %> </h3>
  <%= link_to "link", bird_path(bird) %>
<% end %>

<!--
link_to(body, url, html_options = {})
link_to -->
